# Brandon's App — Owner Decisions Register

> The durable owner-decision ledger for Brandon's App (mirrors the Blueprint Fitness `AWAITING_PETE.md` pattern). Separate from Blueprint Fitness. Every directed item, owner decision, and blocker tracked here so nothing is dropped and context survives session death.

## DIRECTED (owner greenlit; track until a lane is executing)
- 🛑🛑 **CORRECTION — "BUILT" IS PAUSED (Pete 2026-07-01 12:14):** Pete dogfooded the preview + judged it a SKELETON (chat+report+queue+poke), NOT a faithful reusable-harness proof. Real gaps he saw: chat INPUT hidden behind the Android nav/keyboard (awkward/unusable); chat MISSING mic + image/file/share; report flow not the full BP stack; NO floating dev-tool layout; MISSING full bug-report stack, mic-to-report, issue-list behavior, dogfood checklist, full command-center tools. His point: the power is the WHOLE owner/operator system working together; a skeleton doesn't prove reusability. **DO NOT call the port "built"; NO more implementation until the parity map + chunked plan are approved.** ▶ **MAPPING WORKFLOW RUNNING (`we1wdyl09`)**: 7 parallel area-mappers (floating-overlay/chat/report/queue/dogfood/diagnostics/backend-loop) anchored to REAL BP code + a UX/parity validator → `docs/working/JUL01_harness_parity_map.md` = full BP harness inventory + feature-by-feature parity table (BP→ST-now→required) + classification (must-have/adapt/defer/dont-port) + all preview gaps + chunked build plan + validator checklist + the exact chat-input-hidden fix + must-have-vs-deferred verdict on mic/image/screenshot/logs/dogfood. Bring map+recs to Pete BEFORE more build; then approve chunked agents to close gaps.
- ⭐⭐ **DIRECTION REFRAMED (Pete 2026-07-01 02:41): the real goal = PROVE THE HARNESS REUSES IN A SECOND APP.** Not "a Stock-Track app that opens", not App Distribution. Dogfood the BP owner/operator HARNESS *inside Stock-Track*: in-app orchestrator chat + bug/report flow + mic/voice (if in the pattern) + dogfood/testing controls + owner report queue + dev/diagnostics surface + chat action-buttons + the minimal backend so the orchestrator can READ/RESPOND through Stock-Track + docs (clearly Stock-Track's harness, not BP copied blindly). Pinned to Brandon's Firebase (easy-stock-track), MOCK inventory data fine (NOT the real-DB swap). **App Distribution is NOT the blocker — direct APK is fine; Brandon's tester setup waits** (do not gate the harness dogfood on it unless truly required). Acceptance: "Pete opens Stock-Track and can USE the owner chat/report/dogfood controls in it." ✅ **PLAN DELIVERED** (lane a7c2a6b2, `docs/working/JUN30_stocktrack_harness_port_PLAN.md` `7ca6861`, pushed + doc sent). KEY FINDINGS: **Q4 App Distribution NOT needed — direct APK enough** (separate build-DELIVERY from harness-BACKEND). **Port = ~80% clean-copy + regenerate-config, ~20% thin-seam** (BP in-app harness already config-driven from Stage-A: chat 25 modules + report_queue 12 read generated HarnessConfig; owner UID = runtime auth uid; strip 5 BP couplings: IntakeCardStyling/RouteRegionClassifier/CurrentRouteTracker/AuthService/bp_voice). **RUNG LADDER (smallest-dependency-first):** Rung0 = harness behind MockChatRepository (ZERO Brandon dep, proves port+renders+usable) · **Rung1 [REC first APK] = client-persisted to easy-stock-track** (real persistence + live report round-trip; needs only ~10-min Brandon enable: Firestore + Anonymous Auth + rules; NO keys, NO App-Dist, NO orchestrator-access blocker) · Rung2 = orchestrator two-way via `stocktrack_chat.js` (ADC, needs Brandon IAM-grant an ops identity — permissions-only, no key/token) = completes the goal. DEFERRED: voice, 4 export/dashboard chat buttons, tagging/lanes, dashboard/vision projections, push, App-Dist. ✅ **SECURITY FIX DONE (orch): Stock-Track `.gitignore` now excludes admin secrets** (service-account/.env/keystores/key.properties/ADC/CI-tokens) before any backend wiring; client configs stay committed intentionally. ✅ **FULL EXECUTION APPROVED (Pete 2026-07-01 03:05): "proceed with the full harness-port execution, not just a partial/local demo."** ✅ **BUILT + SELF-PROVEN + ORCH-VERIFIED** (lane a637b5dc, pushed `6b682e6`, 42 files/25 harness modules). PROOF: analyze 0 issues · 13/13 tests · direct APK built (211MB, com.stocktrack.app, no App-Dist) · **SEPARATION orch-re-verified: anti-leak scan PASS 0 BP literals/31 files, ZERO BP project-id, ZERO BP UID; only shared token = the generic collection NAME `orchestratorChat` living in easy-stock-track (the PATTERN, not a leak); bp_guard.js 6/6, orchestrator --selftest 5/5** · config→easy-stock-track/stockIssueReports/Stock-Track · 5 BP couplings stripped (IntakeCardStyling→HarnessTheme.accent, AuthService→anon FirebaseAuth, RouteRegionClassifier/CurrentRouteTracker dropped, voice omitted) · `kHarnessMode` picks mock(Rung0)/firebase(Rung1 default) trio. `stocktrack_chat.js` (ADC, projectId-pinned, auto-discovers anon owner thread). ⛔ **FULL CLOUD LOOP NOT PROVEN — blocked-on-Brandon** (persist + orchestrator read/respond untested end-to-end; no easy-stock-track access here by design; lane did NOT falsely claim it). ✅ **`FOR_BRANDON_harness_backend.md` READY + SENT to Pete** (plain-English, security-first: never send key/token/SA; 4 steps = enable Firestore + Anon Auth + deploy committed firestore.rules/storage.rules + add ops email as IAM `roles/datastore.user` [+`storage.objectAdmin`]). Env setup blockers flagged (npm i firebase-admin in scripts/; ADC login). ✅ **PREVIEW APK SHIPPED (Pete-approved)** — `stocktrack-harness-PREVIEW-mock-local.apk` (211MB, mock mode, flag reverted uncommitted, tree clean) sent via file-download; Pete can USE the ported harness offline (command center + chat[2 seeded] + report + queue[1 seeded, full triage], no backend). Awaiting his dogfood. ✅ **Storage-Object-Admin CONFIRMED conditional** (only if screenshots/Storage enabled; text chat+reports work without). ✅ **OPS IDENTITY DECIDED (Pete 2026-07-01): use Pete's Google account `peter.holmes.mitra@gmail.com`** (no dedicated account; rotate later if wanted). ✅ **STORAGE OFF for first proof (Pete): text chat + text reports only; NO Storage enable, NO Storage Object Admin role — screenshots deferred to a later 2nd step.** ✅ **FOR_BRANDON doc FINALIZED + SENT** (Pete's email in Step 4, only `roles/datastore.user`, firestore.rules only, Storage removed, screenshot promise removed) — ready for Pete to forward. Brandon's job = 3 steps: enable Firestore + enable Anon Auth + add Pete's email w/ datastore.user (orch deploys rules). ⏳ **AWAITING: Brandon does his 3 steps** → then orch: one-time ADC browser login (Pete's account, no key) + deploy firestore.rules + prove full loop (persist in easy-stock-track + orchestrator read/respond) → independent VALIDATOR before 'done'. (Preview APK already shipped for Pete's dogfood.) THEN: Brandon does ~10min → full-loop on-device proof → **independent VALIDATOR review before 'done'** (acceptance: installable APK + on-device chat/report/queue + data-in-easy-stock-track + orchestrator read/respond). Building all 8: (1) in-app orchestrator chat, (2) bug/report flow, (3) report queue + dogfood controls, (4) command-center action buttons, (5) Firebase persistence in easy-stock-track, (6) orchestrator read/respond loop (`stocktrack_chat.js`, ADC), (7) direct APK, (8) separation proof. Constraints: mock inventory stays, NO App-Dist, ZERO BP crossover, **auth = permission-only (dedicated ops account + Brandon IAM grant + ADC; NO service-account/keys/tokens/creds)**. Rung-0 mock OK for internal dev but NOT the proof — owner-facing proof = FULL LOOP (open APK → use chat/report/dogfood → data persists in easy-stock-track → orchestrator reads+responds). **SEQUENCE:** (1) lane writes EXACT plain-English Brandon steps `FOR_BRANDON_harness_backend.md` (enable Firestore + Anon Auth + deploy rules + grant ops-identity min IAM; permissions-only) → orch relays to Pete BEFORE Brandon acts; (2) code+APK build in parallel; (3) Brandon does ~15min; (4) full-loop on-device proof; (5) **independent VALIDATOR review before 'done'** (Pete-required). ACCEPTANCE: installable APK + on-device chat/report/queue evidence + data-in-easy-stock-track proof + orchestrator read/respond proof. ✅ gitignore secret-block already done. Nothing claimed proven until tested.
  - 📌 **App-Distribution/tester-pipeline = DE-PRIORITIZED** (was the prior blocker; now LATER at Brandon's pace). The wired Firebase core (6f04b9c) + the `FOR_BRANDON_enable_tester_pipeline.md` note stay VALID for when Brandon's ready — just no longer the critical path. NOTE: the HARNESS backend (chat/report Firestore collections) may still need Brandon's Firestore DB + orchestrator access — the planning lane addresses that as its own (smaller) dependency, distinct from App Distribution.
- ⭐ **BRANDON'S FIREBASE PROJECT ARRIVED — THE UNLOCK** - Pete 2026-07-01 (sent both configs). Brandon created his OWN Firebase project **`easy-stock-track`** + registered the app (`com.stocktrack.app`). Both configs orch-VERIFIED (identifiers only, no secrets printed): `google-services.json` (Android, the buildable target) + `GoogleService-Info.plist` (iOS, for a Mac later); STORAGE_BUCKET `easy-stock-track.firebasestorage.app`. **Separation CLEAN** — project = easy-stock-track, NOT `blueprintfitnesssubscriptions`; configs are client-connection configs (not admin secrets). ▶ WIRING LANE (ae6be9a7) EXECUTING in the Stock-Track repo: place both configs → wire Firebase CORE (Android google-services plugin + firebase_core + initializeApp) keeping MOCK data this step → build-prove → stand up the ship pipeline pinned to easy-stock-track → App Distribution `stocktrack-testers` (may be BLOCKED-on-Brandon: App Distribution enable + tester group + his auth to upload). NEVER a BP credential/project. Unlocks: real tester pipeline + real data + Brandon's in-app chat. NEXT after core: mock→Firestore data swap (separate slice).
  - ✅ **FIREBASE CORE WIRED + BUILD-PROVEN** (lane ae6be9a7, 2026-06-30). Placed both configs (`android/app/google-services.json`, `ios/Runner/GoogleService-Info.plist`); wired the google-services Gradle plugin (`settings.gradle.kts` classpath + `android/app/build.gradle.kts` apply), added `firebase_core 4.11.0`, `Firebase.initializeApp()` in `lib/main.dart`. Android `minSdk` 21→23 (firebase_core 4.x requirement), NDK pinned to 27.0.12077973. **DATA STILL MOCK** — repo abstraction untouched (mock→Firestore swap = separate slice). PROOF: `flutter analyze` clean; 4/4 tests pass; `flutter build apk --debug` SUCCEEDS (187MB fat debug); the gms plugin injected `project_id=easy-stock-track` into app resources, `FlutterFirebaseCorePlugin` registered, firebase native libs bundled in the APK. On-device `Firebase.initializeApp()` runtime confirmation = pending Pete/Brandon dogfood (all init prerequisites proven present). **Separation CLEAN** — zero BP identifiers in any functional config (only doc guardrails + Xcode's standard 'Blueprint' scheme keys). **SHIP PIPELINE:** `scripts/stocktrack_ship.sh` (pinned to easy-stock-track + Brandon's app id + `stocktrack-testers`, agent-first PASS/FAIL/BLOCKED, uses Windows firebase CLI since WSL hangs, defensive BP-identifier abort) + `docs/STOCKTRACK_SHIP_RUNBOOK.md`. ⛔ **UPLOAD BLOCKED-on-Brandon** (verified: the only Firebase auth here = Pete's `peter.holmes.mitra@gmail.com`, which sees ONLY BP's project, NO access to `easy-stock-track`). Brandon must: (1) enable App Distribution, (2) create `stocktrack-testers` group (add Pete+Brandon), (3) grant an uploader access to easy-stock-track (grant Pete's account App-Dist-Admin, OR ship from his own machine, OR provide his own CI token). Never a BP credential. NEXT slice: mock→real Firestore data swap (add `cloud_firestore`, write `FirebaseInventoryRepository`, flip the 2 provider-override lines in `main.dart`).
- **Repo wiring -> corbett998-netizen/Stock-Track** - Pete 18:37/18:55. ✅ DONE: pushed to origin/main `7ad81f2` (remote initial README + history preserved, all starter docs merged, README product-fixed to HVAC, branch->main). Write access works via Windows credential mgr.
- **MVP / product-architecture SPEC** - DIRECTED Pete 18:55 (coordinated gated plan, NOT rush-to-code). ✅ SPEC DONE (lane ac512471, `docs/working/JUN30_stocktrack_MVP_architecture_SPEC.md` 13baeba, pushed to Stock-Track): first slice = Inventory+Dashboard+Scan; entities products/categories/locations/installers/installations/customers/recalls/users; Flutter+Riverpod+Firebase(Brandon's OWN)+phone-cam scan; 9 owner-calls C1-C9 w/ recs; gated build seq. ✅ SPEC APPROVED by Pete 19:40 — all 9 calls locked (Firebase=Brandon's-own, phone-cam scan, slice1=Inventory+Dashboard+Scan, single-company, simple-roles-reserved, recalls=flag->affected-list, truck-implicit, customer-free-text-first, greenfield). Shell PLAN done (ad99d561, 966499e1). ⭐ REALIGNED by Pete 19:47 -> FRONTEND-FIRST: first APK does NOT need Firebase. BUILDING the real on-device shell NOW with MOCK/local data + repository abstraction (InventoryRepository interface -> MockRepo now, FirebaseRepo later, UI untouched) via lane abfea448. First-APK goal: opens on phone, dark Stock-Track look, nav, Dashboard+Inventory+Scan, placeholder data + low-stock, scan stubbed, NO BP config/secrets. Security boundary INTACT (no BP, no cloud this slice; Brandon's own Firebase swapped in LATER via documented clean swap). ✅ SLICE 1 BUILT + DELIVERED: Flutter app (com.stocktrack.app, mock data, repo abstraction, 3 screens, dark theme, scan stub) e741ed2 pushed to Stock-Track; analyze clean + 4/4 tests + APK built (86MB debug). Orch-VERIFIED no BP/firebase/secrets (only swap-path comments + Xcode's standard 'Blueprint' scheme keys). APK delivered to Pete's phone (file-download link) + MOCKED_VS_REAL.md. AWAITING Pete's on-device dogfood. NEXT: gradual Firebase (Brandon's own project) via the single-switch clean swap, documented.
- **Set up the separate Brandon's App project space** — DIRECTED by Pete 2026-06-30 17:19. ✅ DONE: repo `/mnt/c/dev/Brandons_App` created (separate git repo, adjacent to Blueprint Fitness) with README + this register + OWNER_VISION starter + working/ + handoff + ORCHESTRATION_NOTES. No coding. Awaiting owner's first product direction.

- **First product direction received** — Pete 2026-06-30 17:53. ✅ CAPTURED: product CORRECTED to **HVAC/electrical equipment inventory + tracking app** (NOT stock-trading), cloud-based, Flutter iOS + Android. 3 reference screenshots (Dashboard / Inventory / Installation-History, "StockTrack Warehouse" style) inspected + saved durably to `references/`. OWNER_VISION updated. Visual references, not final requirements.

- **Distribution pipeline PLAN** - DIRECTED Pete 20:17 (Stock-Track needs its OWN clean build/test/distribution path, not chat-file-download forever). ✅ DONE (lane a50715105, `docs/working/JUN30_stocktrack_distribution_plan.md` 7f2906f7, pushed + delivered). 4-stage graduation; guardrail = every ship cmd pinned to Brandon's project/app (can't touch BP); finding = tester pipeline needs only Brandon's Firebase + app-registered, NOT the cloud-data swap. ⛔ UNLOCK for distribution graduation AND real-data = the same one thing: Brandon's OWN Firebase project (orch offered Pete the setup walkthrough).

- **Owner-comms HARNESS plan** - DIRECTED Pete 20:37 (Brandon needs his OWN owner-comms/dev-chat harness to operate as a non-coder, same capability as BP, never BP infra). EXECUTING via lane a44fe78d -> `docs/working/JUN30_stocktrack_harness_plan.md`: 7 areas (need-it / minimum / reuse-pattern-not-config / form[in-app vs local vs doc vs staged] / backend[mock-vs-Brandon-Firebase, same-project-separate-collection vs isolated] / separation-list / next-APK-sequence). Recurring unlock = Brandon's Firebase (real in-app chat needs Firestore). PLAN ONLY.

## AWAITING-OWNER-DECISION (asked; needs the owner's call)
- ⭐ **SHIP-PIPELINE UNBLOCK — PATH DECIDED (Pete 2026-07-01): A (grant Pete's account access via Firebase/GCP PERMISSIONS only).** Pete's security rule: Brandon must NOT share any admin keys, service-account keys, passwords, CI tokens, or private credentials — access is granted ONLY through Firebase/Google Cloud IAM permissions. → This RULES OUT option C (CI token = a shared credential) and selects **A**: Brandon adds `peter.holmes.mitra@gmail.com` as **Firebase App Distribution Admin** in his console (Users & permissions), then orch ships from here (already authed as Pete). ✅ **"For Brandon" note WRITTEN** (`docs/FOR_BRANDON_enable_tester_pipeline.md`) — plain-English 3 steps (enable App Distribution → create `stocktrack-testers` group +Pete +Brandon → grant access via permissions) with a **prominent SECURITY-first block: never share keys/SA/passwords/tokens; permissions-only**. Sent to Pete to forward. ⛔ Still BLOCKED-on-Brandon until he does the 3 console steps. Firebase core already wired + build-proven (6f04b9c). Never a BP credential.
- **Debug-signed internal builds now vs Brandon's own release keystore** (long-lead/permanent) — debug fine for internal testers; keystore later.
- **minSdk now 23** (Android 6.0+, ~99% coverage) — forced by firebase_core 4.x; flag only if it matters.
- 8 open questions to move from references → buildable spec — see `OWNER_VISION.md` "Open questions for the owner" (backend/cloud choice, scanning method, multi-user/roles, recall tracking, truck-as-location, customer records, greenfield-or-existing, first slice + timeline).

## BLOCKED
- (none.)

## RESOLVED (history)
- 2026-06-30 17:19 — project SETUP directed + completed (separate space established, harness pattern reused, separation boundary documented).
